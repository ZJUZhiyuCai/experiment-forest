# 🚀 实验小森林 - 部署指南

## 📋 项目特点
- ✅ **纯前端应用** - 无需后端服务器
- ✅ **数据本地存储** - 使用浏览器localStorage
- ✅ **已配置Vercel** - 有 `vercel.json` 配置文件
- ✅ **静态站点** - 构建后是纯静态文件

---

## 方案一：🌐 Vercel 部署（最推荐）

### 优点
- ⚡ **免费** - 每月100GB流量
- 🚀 **全球CDN** - 访问速度极快
- 🔧 **零配置** - 自动HTTPS
- 📱 **自动部署** - Git推送即部署

### 步骤

#### 1. 准备项目
```bash
# 确保项目可以正常构建
cd D:\my-projects\实验小森林\63686441218
npm run build
```

#### 2. 推送到 GitHub
```bash
# 如果还没有Git仓库
git init
git add .
git commit -m "Initial commit"

# 在GitHub创建新仓库后
git remote add origin https://github.com/你的用户名/实验小森林.git
git branch -M main
git push -u origin main
```

#### 3. 在 Vercel 部署
1. 访问 [vercel.com](https://vercel.com)
2. 使用 GitHub 账号登录
3. 点击 **"New Project"**
4. 选择刚才的 GitHub 仓库
5. Vercel 会自动识别配置（因为已有 `vercel.json`）
6. 点击 **"Deploy"** 按钮
7. 等待1-2分钟，部署完成！
8. 你会得到一个 URL：`https://你的项目名.vercel.app`

### 更新部署
```bash
# 每次修改代码后
git add .
git commit -m "更新内容"
git push
# Vercel 会自动重新部署！
```

---

## 方案二：📦 Netlify 部署

### 优点
- 🆓 **免费** - 每月100GB流量
- 🌍 **全球CDN** - 多节点分布
- 🔐 **自动HTTPS** - SSL证书自动配置
- 🎨 **表单处理** - 免费表单功能

### 步骤

1. 访问 [netlify.com](https://www.netlify.com/)
2. 注册并登录（推荐使用GitHub账号）
3. 点击 **"Add new site"** → **"Import an existing project"**
4. 选择 GitHub 仓库
5. 配置构建设置：
   ```
   Build command: npm run build
   Publish directory: dist
   ```

6. 点击 **"Deploy site"**
7. 部署完成！你会得到 `https://你的项目名.netlify.app`

---

## 方案三：🔧 GitHub Pages 部署

### 优点
- 🆓 **完全免费** - GitHub提供的静态站点服务
- 📚 **与仓库集成** - 直接连接到GitHub仓库
- 🌐 **自定义域名** - 支持自定义域名

### 步骤

#### 方法A：使用 GitHub Actions（推荐）

1. 创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Build
      run: npm run build

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
```

2. 推送到 GitHub，Actions 会自动部署

3. 在 GitHub 仓库设置中启用 Pages：
   - 进入 **Settings** → **Pages**
   - Source 选择 **gh-pages branch**
   - 访问 `https://你的用户名.github.io/仓库名`

#### 方法B：手动部署

```bash
# 1. 安装 gh-pages
npm install -g gh-pages

# 2. 构建项目
npm run build

# 3. 部署到 gh-pages 分支
gh-pages -d dist

# 4. 在 GitHub 设置中启用 Pages
```

---

## 方案四：☁️ 阿里云/腾讯云 OSS

### 优点
- 🇨🇳 **国内访问快** - 如果主要用户在国内
- 💰 **按量付费** - 流量费用低
- 🔒 **安全可靠** - 大厂服务保障

### 步骤（以阿里云OSS为例）

1. 登录阿里云控制台
2. 开通 **对象存储 OSS** 服务
3. 创建 **Bucket**（存储桶）：
   - 名称：例如 `lab-forest`
   - 读写权限：**公共读**
4. 上传文件：
   ```bash
   # 方法1：通过控制台上传 dist 目录所有文件
   # 方法2：使用 ossutil 工具
   ```

5. 配置静态网站托管：
   - 进入 Bucket 设置
   - 开启 **静态网站托管**
   - 默认首页：`index.html`
   - 默认404页：`index.html`（重要：支持React Router）

6. 绑定自定义域名（可选）

---

## 方案五：🖥️ 本地服务器部署

### 适用场景
- 🏢 **内网环境** - 局域网内使用
- 🔒 **数据安全** - 不想上传到外网
- 💻 **离线使用** - 无需互联网

### 步骤

#### 方法1：使用 Python（最简单）

```bash
# Python 3
cd D:\my-projects\实验小森林\63686441218\dist
python -m http.server 8000

# 访问：http://localhost:8000
# 局域网访问：http://你的IP:8000
```

#### 方法2：使用 Node.js (http-server)

```bash
# 安装
npm install -g http-server

# 启动
cd dist
http-server -p 8000

# 访问：http://localhost:8000
```

#### 方法3：使用 Nginx（生产环境推荐）

1. 安装 Nginx
2. 配置 `nginx.conf`：

```nginx
server {
    listen 80;
    server_name localhost;
    root /path/to/dist;
    index index.html;

    # 重要：支持 React Router
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 开启 gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

3. 启动 Nginx

---

## 方案六：🐳 Docker 部署

### 优点
- 📦 **环境一致** - 避免环境问题
- 🔄 **易于迁移** - 跨平台部署
- ⚙️ **易于管理** - 统一管理

### 步骤

1. 创建 `Dockerfile`：

```dockerfile
# 构建阶段
FROM node:18-alpine as build

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# 生产阶段
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

2. 创建 `nginx.conf`（见方案五）

3. 构建镜像：

```bash
docker build -t lab-forest .
```

4. 运行容器：

```bash
docker run -d -p 8080:80 --name lab-forest lab-forest
```

5. 访问 `http://localhost:8080`

---

## 📊 部署方案对比

| 方案 | 难度 | 费用 | 速度 | 推荐场景 |
|------|------|------|------|----------|
| **Vercel** | ⭐ | 免费 | ⭐⭐⭐⭐⭐ | 个人项目、快速部署 |
| **Netlify** | ⭐ | 免费 | ⭐⭐⭐⭐⭐ | 静态站点、表单功能 |
| **GitHub Pages** | ⭐⭐ | 免费 | ⭐⭐⭐⭐ | 开源项目 |
| **阿里云OSS** | ⭐⭐⭐ | 低价 | ⭐⭐⭐⭐ | 国内用户 |
| **本地服务器** | ⭐ | 免费 | ⭐⭐⭐ | 内网、开发测试 |
| **Docker** | ⭐⭐⭐⭐ | 服务器费用 | ⭐⭐⭐⭐⭐ | 生产环境 |

---

## ⚙️ 部署后配置

### 1. 数据备份提醒
由于数据存储在浏览器localStorage，建议：
- 定期导出数据（设置 → 数据管理 → 导出）
- 提醒用户备份数据

### 2. AI 配置
在设置中配置 AI API：
- API端点：例如 `https://api.siliconflow.cn/v1/chat/completions`
- API密钥：从服务商获取
- 模型：例如 `qwen2.5-72b-instruct`

### 3. 自定义域名（可选）
- **Vercel**: 在项目设置中添加域名
- **Netlify**: 在 Domain settings 中添加
- **GitHub Pages**: 在仓库设置中配置自定义域名

---

## 🐛 常见问题

### 1. 刷新页面404
**原因**: React Router 的路由模式
**解决**: 确保服务器配置了 SPA fallback（所有请求返回 index.html）

### 2. 静态资源加载失败
**检查**: `vite.config.ts` 中的 `base` 配置
```typescript
base: './'  // 相对路径，适用于大多数情况
// 或
base: '/你的子目录名/'  // 如果部署到子目录
```

### 3. 构建后本地无法打开
**解决**: 静态站点需要HTTP服务器，不能直接双击 `index.html`

### 4. AI功能无法使用
**原因**: API配置问题或网络问题
**解决**: 检查设置中的API配置

---

## 🎯 推荐部署流程

### 对于个人使用
1. 本地运行：`npm run dev`
2. 或局域网部署：使用 Python http-server

### 对于公开分享
1. **首选**: Vercel（免费、快速、简单）
2. **备选**: Netlify
3. **国内用户**: 阿里云OSS

### 对于团队使用
1. **小团队**: Vercel/Netlify + 定期数据备份
2. **大团队**: 需要后端API + 数据库（需要重新开发）

---

## 📞 需要帮助？

- 📧 作者邮箱: 3210102604@zju.edu.cn
- 📖 查看文档: `产品说明书.md`, `安装使用指南.md`
- 🐛 问题反馈: GitHub Issues

---

**祝部署顺利！🎉**

*实验小森林 - 让科研记录变得简单有趣*
